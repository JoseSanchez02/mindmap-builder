services:
  - type: web
    name: mindmap-builder
    env: node
    plan: free
    nodeVersion: 18
    buildCommand: npm run build:all
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
